# Ecommerce Agent System 开发与学习进度计划

> 更新日期：2026-08-18

## 1. 计划基准

本项目同时承担产品实现和 Python 学习两个目标，开发方式分为：

- 前端：由 Codex 对话直接实现，用户主要负责需求确认、视觉检查和前后端联调验收。
- 后端：采用教学模式。用户亲自编写生产业务代码，Codex 每次提供一个小步骤，并负责讲解、审查、自动化测试、Mock、验证和学习文档。
- 单次学习时间：固定按 3 小时统计和安排。
- 推荐频率：每周 6 次，保留一天用于休息、复盘或处理计划外问题。

由于后端采用教学模式，整体速度会比直接代写慢约 30%～50%，但这部分时间计入 Python、FastAPI、数据库和 Agent 工程的有效学习时间。

按当前已完成内容和新增的正式用户认证范围重新估算，整个项目预计还需要约
**125～180 个有效小时**：

- 固定 3 小时/次：约 42～60 次学习班。
- 每周 6 次时，理论投入约为 18 小时；加入教学、复盘、返工和计划外问题缓冲后，正常目标仍为 10～14 周，安全目标为 16 周。
- 当前没有连续记录每次学习班的实际用时，因此剩余工时按各版本范围估算，不用未经记录的时间制造精确进度。

## 2. 当前状态

V0.1 已完成：

- Turborepo、Next.js 和 FastAPI 基础目录。
- 前端工作区 Shell、共享 UI 与基础 Motion 动效。
- FastAPI 应用工厂、lifespan、配置、异步数据库 Engine 和健康检查。
- `POST /v1/chat`、Pydantic 输入校验、ChatService、ChatModel Protocol 和 ChatOpenAI Adapter。
- OpenAI-compatible Base URL、模型、reasoning effort、Responses API 和请求超时配置。
- 稳定的聊天错误码、上游异常脱敏和不记录完整用户消息的日志边界。
- 可交互聊天工作台、Next.js `/api/chat` 代理、运行时响应校验、失败状态和重试交互。
- 后端学习文档 `docs/learning/v0.1-backend.md`。
- 后端 Pytest 最近验证为 **51 passed**；前端 Vitest 本次验证为 **21 passed**。
- 用户已在真实浏览器中完成前端、Next.js 代理、FastAPI 和实际 AI 服务的端到端验收。

当前 V0.1 完成度为 **100%**。工作区中仍有前端未提交文件，但这属于版本管理收尾，不影响功能验收结论。

V0.2 后端已完成：

- Prisma 管理 `User / Order / ShipmentEvent`、两份 migration 和中文 demo seed；SQLAlchemy
  只读，不创建 Prisma 所有的表。
- `GET /v1/orders/{order_id}` 返回订单基本信息、精确金额和按时间排序的物流轨迹。
- SQL 同时按 `order_id + user_id` 过滤；他人订单和不存在订单统一返回 `404`。
- 服务端 `DEMO_USER_ID` 是临时身份边界，调用方不能通过 path、query 或 header 切换用户。
- 数据库异常映射为脱敏 `503`，日志不记录原始 SQLAlchemy 异常或连接信息。
- HTTPX Route 测试、Fake Engine/Connection 测试和隔离数据库集成测试已建立。
- 学习记录见 `docs/learning/v0.2-backend.md`。

V0.2 明确未包含：

- 正式登录、JWT、用户可切换身份和会话归属。
- 订单写入、库存、退款或模型调用订单工具。
- LangGraph、Checkpoint、SSE 和 RAG。

当前 V0.2 后端闭环为 **100%**。前端订单展示和完整浏览器联调若尚未验收，仍作为产品层
收尾，不改变后端完成结论。

V0.3 后端已完成：

- LangGraph 角色化消息状态与完整历史模型输入。
- 可选 `thread_id` 的无状态/有状态兼容请求链。
- PostgreSQL Checkpoint、跨应用生命周期恢复和 LangGraph 自有表边界。
- `request_id` 幂等、条件分支、失败重试和同进程并发顺序保护。
- Fake/Mock 单元测试及隔离 PostgreSQL 集成测试。
- 学习记录见 `docs/learning/v0.3-backend.md`。

V0.3 后端闭环为 **100%**。多 Worker 协调、JWT 会话归属和长期记录清理仍属于后续版本。

## 3. 用户、会话与认证计划

项目目前存在数据库 `User`，但尚不存在正式登录用户。三个标识不能混为一谈：

| 标识 | 当前/计划用途 | 是否是可信用户身份 |
|---|---|---|
| `DEMO_USER_ID` | V0.2 服务端固定的订单演示身份 | 否 |
| `thread_id` | V0.3 标识一段 LangGraph 对话和 Checkpoint | 否 |
| JWT `sub` / user claims | V0.4 认证后的当前用户及资源归属 | 是 |

V0.3 只接入对话状态和恢复：请求通过 `thread_id` 找到 Checkpoint，但 `thread_id` 不证明
请求者是谁，也不能单独用于订单授权。V0.3 继续处于 demo/匿名范围，聊天不会调用订单工具。

正式用户接入安排在 **V0.4 的第一阶段**，并且必须早于 Router、订单 Tool Calling 和 SSE：

- 前端取得并携带 JWT；Agent Core 验证签名、有效期和必要 claims。
- `get_current_user` dependency 从可信 claims 生成当前用户身份，替换 `DEMO_USER_ID`。
- Checkpoint 与对话查询同时按 `user_id + thread_id` 归属，不能仅凭 thread ID 读取会话。
- 订单工具的 `user_id` 只能来自认证上下文，不能由模型参数或调用方直接指定。
- 未认证请求返回 `401`；已认证但资源不属于当前用户时保持不可枚举的资源边界。

选择 V0.4 而不是 V0.5 的原因是：V0.4 开始让 Agent 调用用户订单工具，必须在此之前建立
可信身份；等退款版本再补认证会使 V0.4 的用户数据访问缺少安全前提。

## 4. 版本量化表

| 版本 | 核心成果 | 剩余预计小时 | 当前状态 | 累计工作量 |
|---|---|---:|---:|---:|
| V0.1 | FastAPI + OpenAI 基础聊天闭环 | 0h | 100% | 6% |
| V0.2 | PostgreSQL、订单和物流查询 | 0h（后端） | 后端 100% | 15% |
| V0.3 | LangGraph State、Checkpoint 和会话恢复 | 0h（后端） | 后端 100% | 27% |
| V0.4 | JWT 用户认证、会话归属、Router、多 Agent、Tool Calling 和 SSE | 26～36h | 未开始 | 42% |
| V0.5 | 退款风控、客户确认和人工审批 | 24～34h | 未开始 | 60% |
| V0.6 | PDF、pgvector、混合检索和引用 | 26～38h | 未开始 | 79% |
| V0.7 | Worker、任务恢复、Outbox 和并发可靠性 | 30～44h | 未开始 | 100% |

## 5. 推荐周计划

以下计划按照每周 6 次、每次固定 3 小时估算，并包含学习复盘和返工缓冲。当前从 V0.4
开始起算：

| 周期 | 目标 | 阶段成果 |
|---|---|---|
| 已完成 | V0.3 | 掌握 LangGraph 状态、Checkpoint 与会话恢复 |
| 当前 / 第 1～3 周 | 完成 V0.4 | 接入正式用户并形成可展示的 Multi-Agent Demo |
| 第 6～7 周 | 完成 V0.5 | 跑通退款和人工审批闭环 |
| 第 8～10 周 | 完成 V0.6 | 完成企业知识库 RAG |
| 第 11～14 周 | 完成 V0.7 | 完成企业级可靠性加固 |

关键里程碑：

- 已完成：最小 AI 客服通过真实浏览器联调。
- 已完成：只读订单、物流查询与 demo 用户归属边界。
- 已完成：状态持久化完成，具备项目核心技术含量。
- 第 5 周：JWT 用户归属 + 多 Agent + Generative UI，可作为阶段性作品展示。
- 第 7 周：退款审批闭环完成，达到业务 MVP。
- 第 10 周：RAG 完成，覆盖主要业务目标。
- 第 14 周：可靠性加固完成，形成完整项目。

## 6. 单次学习班安排

### 3 小时固定学习班

| 环节 | 时间 | 内容 |
|---|---:|---|
| 回顾 | 15 分钟 | 确认当前状态和本次验收目标 |
| 概念 | 35 分钟 | 讲解请求链、Python 概念和设计原因 |
| 编码 | 90 分钟 | 用户完成后端业务代码 |
| 验证 | 30 分钟 | 自动化测试、类型检查和调试 |
| 复盘 | 10 分钟 | 总结知识点并更新学习文档 |

前端任务可以在后端学习过程中异步推进，但每个版本仍应预留约 15%～20% 的时间用于接口契约确认、页面检查和联调验收。

## 7. 版本完成度计算

每个版本按以下权重统计：

| 项目 | 权重 | 完成标准 |
|---|---:|---|
| 后端知识点理解 | 10% | 能描述主要请求链和关键设计原因 |
| 后端业务代码 | 35% | 用户完成本版本生产业务代码 |
| 自动化测试与错误处理 | 20% | 正常、错误和关键边界测试通过 |
| 前端实现与联调 | 20% | 页面能够消费真实接口并正确展示状态 |
| 手动验收与学习复盘 | 15% | 完成验收场景和版本学习记录 |

只有五部分全部完成，版本才标记为 100%。不以“文件已经创建”或“接口可以返回一次结果”作为版本完成标准。

## 8. 每版验收清单

每个版本结束前必须满足：

- 本版约定的业务闭环可以演示。
- 自动化测试不调用真实付费 API。
- 相关测试、Lint 和变更范围类型检查通过。
- 前后端接口契约一致。
- 服务重启、失败路径或权限边界按本版风险完成验证。
- 学习文档记录本版 Python 和 AI 知识点。
- 明确记录未覆盖场景和下一版本边界。

## 9. 计划更新规则

- 每完成一个版本，更新当前状态、实际工时和偏差原因。
- 如果实际工时超出上限 30%，先判断是学习深挖、需求扩张还是技术阻塞，不直接压缩测试时间。
- 新功能只能进入当前版本明确范围或后续版本待办，不应临时插入正在学习的后端步骤。
- 前端提前完成不会自动提高版本完成度，必须等待后端、测试、联调和复盘共同完成。
- 时间不足时优先保证当前版本闭环，企业级增强留到 V0.7，不跨版本提前铺设。
